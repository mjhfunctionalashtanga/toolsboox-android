# HANDOFF — 2026-08-13

Canon. Byte-identical in both repos. Supersedes `HANDOFF-2026-08-10.md` and its addendum entirely.

- Android — `~/toolsboox-correspondence`, branch `punchlist-2026-07-26`, at **1.06.59-00**
- iOS — `~/ledger-ipad-correspondence`, branch `correspondence`, at **TestFlight 58** (see numbering below)

## Where things stand

**Android ran 1.06.46 → 1.06.59 in three days** — all published to the R2 URL, whole Boox fleet
(Palma, Go 6, Tab Mini C, Tab X, Tab 8) current as of the last plug-ins. 390 unit tests green.

**iOS ran TestFlight 51 → 58.** 373 SwiftPM tests green. ⚠️ **App Store Connect's build numbers
run ONE AHEAD of the repo's labels** (what the repo called "Build 56" uploaded as ASC 57): ASC is
truth; `CURRENT_PROJECT_VERSION` is now 58 and the next upload is 59. **TestFlight 58 is the
launch-crash rescue and AWAITS MICHAEL'S DEVICE PROOF** — the crasher (ASC 57) died at launch on
real devices from the known SwiftUI metadata stack overflow: `PlannerShell.body`'s own mangled
type name had reached 7,149 characters and blew the device stack during type instantiation
(simulators have bigger stacks and never reproduced it). The fix cut the peak −41% via named
subview extractions + three deliberate AnyView seams (commit `a5641da9`). If 58 still crashes,
the TestFlight crash-share → ASC feedback pipeline is proven and scripted (see Tools below).

**The wire has not moved.** Day-JSON schema untouched through everything. New SYNCED FILES beside
it (all id-union + tombstones, house merge): `listen-state.json`, `reading-state.json`,
`books-index.json` + `books/` (the library hub), all live on dav.mjh.yoga.

## What got built (headlines; commit messages carry the full design record)

1. **The 08-10 handoff's six-step iOS parity — complete.** RitualWalk · IA restructure ·
   LedgerLinks · GramComposer (bit-exact deterministic layout across forks) · Bookshelf pointer ·
   NotesMirror.
2. **Boards & Tasks rethink, both platforms** (`DESIGN-boards-tasks-2026-08-10.md`): today page
   is paper (carry-over dead, OpenTasks ghost rows now self-explaining), one Boards door with
   List⇄Columns + Waiting lane, SchedulePrep proposals-you-accept, TaskWalkCache.
3. **The sync project** (`DESIGN-listen-books-sync-2026-08-11.md`), steps A–D built both
   platforms: Listen playback + reading-position sidecars (pause here, resume anywhere — WORKS,
   Michael verified in the field) · the library hub with add-anywhere-propagates-everywhere ghost
   fetch cards · HubTransport seam with Google Drive backend (users) beside WebDAV (Michael).
   **Step E remains: run hub + Syncthing in parallel ~1 week, then retire Syncthing for books.**
4. **The watch — spoken lens v1**: feeds/mail/book-chapter TTS with karaoke word-highlight
   (1/2/3-word window), write-backs through existing stores only. Best-installed-voice picking +
   the honest boundaries: background speech continues on Bluetooth only; compact Siri is the
   floor until/unless watchOS exposes downloaded voices to apps.
5. **Rich widgets both platforms**: Android scrollable Feed/Mail lists + Task Checklist with a
   REAL done-checkbox (via queue, see 7); iOS at the platform ceiling (XL families, per-row
   links, iOS-17 interactive buttons; WidgetKit cannot scroll — stated, not fudged).
6. **Per-surface multi-site (Android)**: the site switcher is a default, not a lock — config-per-
   call through ~48 bridge functions; Messages/Community/Boards default ATS, Bookings/Roster TYC.
   iOS twin NOT yet ported (`App/LedgerSites.swift` still activate-locked — a queued parity item).
7. **Data-integrity war stories, all fixed at root**: the day-file TEAR (every save shared ONE
   temp filename; concurrent saves interleaved on the inode and the loser kept writing through
   the atomic rename — now unique temps + per-target lock + the widget writes via a drained
   queue, hammer-tested) · the 08-10 stroke transplant (iPad undo replayed a note onto the day
   page; surgically fixed via re-mint + tombstones through the merge fabric) · the Boox landscape
   nav-bar wall · foliate's tap-slop page bounce · the split-view cold-start amnesia (3rd cure:
   verified-arrival, also awaiting Michael's device proof).

## Field-test channels (how Michael reports)

- **Typed**: a text note titled "iOS ledger" (`text-notes/notes-YYYY-MM-DD.json` on the WebDAV
  volume) — read it via ssh (below). This session polled it with a Monitor; re-arm in a new
  session if field-testing resumes.
- **Handwritten**: pages tagged #Ledger #Android (or #iOS) in day-file `noteStrokes` — pull
  `day-YYYY-MM-DD-v2.json` (VPS or adb), render strokes to PNG (PIL, 1404×1872), read the image.

## Open items, ordered

1. **TF58 device proof** (the crash fix) and the cold-start deadlock proof — Michael's hands.
2. **iOS parity queue**: per-surface multi-site (SiteRouting ladder port) · Android's 08-12 fix
   rounds where iOS has twins (history ring, ghost captions, All-Stars mail-open resolver…) ·
   All Stars card geometry (iOS copies Android's new intake numbers — the file says so).
3. **Gram Picks → michaeljoelhall.com publish** — scoped in the 1.06.59 commit: needs a
   GRAM_PICKS surface constant + an element-fed sourceLinks supplier on the provenance stamp.
4. **Sync step E** — the Syncthing retirement week (calendar, not code).
5. **Server piece**: `/crm/campaign` + `/crm/lists` bridge routes (spec in the 1.06.56 commit).
6. **Watch v2**: podcast streaming on the wrist · full chapter extraction · direct WebDAV.
7. **Smaller queued**: hub wifi/ceiling Settings rows · iOS Drive-backend chooser UI · Android-14
   widget row-open trampoline · mail list widget iOS twin · task 17 (the "clobbered notes"
   re-check — server data verified clean; likely the corrupt-file device or a render overlap;
   Michael never confirmed which device).
8. **Banked design pieces**: in-app browser (both, EinkBro?) · free-circle lasso auto-detect on
   iOS · Connection.idFor separator divergence (iOS space vs Android NUL → same edge = two rows
   in connections.json; needs a migration ruling) · NotesMirror's Files-app exposure ruling ·
   Support-under-Fluent ruling · watch as full podcast player.

## Tools & gotchas earned this run

- **Android ship recipe** unchanged (clean worktree, `assembleProdStandardDebug`, rclone to R2,
  `adb install -r` per plugged device). Variant-scoped gradle names; BUILD SUCCESSFUL is the only
  pass signal.
- **iOS**: pbxproj is now GITIGNORED — project.yml is truth; `xcodegen generate` regenerates, and
  any hand-added Info.plist key MUST be declared in project.yml or regen silently drops it (this
  nearly shipped once; Files-app keys now declared). TestFlight from CLI: archive + exportArchive
  with `-allowProvisioningUpdates` and NO API-key flags (Xcode's signed-in account does cloud
  signing; the API key lacks that permission).
- **ASC crash feedback, scripted**: venv at scratchpad `ascenv` (PyJWT), key `B28W2Y3P38`, issuer
  `0b0e2f64-…`, app id `6791343721`; `betaFeedbackCrashSubmissions` → related `crashLog.logText`.
  Michael taps TestFlight's "Share with developer" and the stack arrives in minutes.
- **WebDAV server-side**: `ssh root@72.62.86.151`, volume
  `/var/lib/docker/volumes/webdav_webdav_data/_data/data/` (day files under `ToolsForBoox/json/`
  AND `calendar/Y/M/`; sidecars + hub at the root; text-notes/). Direct server-file surgery LOSES
  races against device pushes — inject fixes into a DEVICE's local file so tombstones enter the
  merge fabric instead.
- **Never let two writers share a temp filename.** The whole tear class.
- API storms (529s): resume agents via SendMessage — transcripts survive; back off, then fresh-
  launch if repeated resumes die in diagnosis.
