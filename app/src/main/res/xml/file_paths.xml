<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera capture target for the long-press "Image — camera" flow. -->
    <cache-path name="camera" path="camera/"/>
    <!-- Panel PNG cards rendered for share / annotation. -->
    <cache-path name="cards" path="cards/"/>
</paths>
